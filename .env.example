PORT=3000
SESSION_SECRET=your_very_secret_key_here_change_this

# PostgreSQL Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=kelasonline
DB_USER=postgres
DB_PASSWORD=your_postgres_password

# App URL (for generating links)
APP_URL=http://localhost:3000

# SMTP Email (optional, used for registration, schedule notifications, and support feedback)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM="Kelas Online Mr.BOB <no-reply@kelasonlinemrbob.com>"
# Recipient of new-registration notifications (defaults to MAIL_FROM/SMTP_USER if empty)
ADMIN_EMAIL=
# Feedback Help & Support recipient
SUPPORT_EMAIL=kelasonline.mrbob@gmail.com
# Admin WhatsApp for the "KLIK DISINI" contact link in confirmation email (e.g. 6281234567890)
ADMIN_WHATSAPP=
# Google Maps location link shown in the confirmation email (optional)
MAPS_URL=

# Google Spreadsheet sync — Apps Script Web App URL (.../exec). Leave empty to disable.
SHEET_WEBHOOK_URL=
# Optional: separate webhook for tutor available time. Defaults to SHEET_WEBHOOK_URL if empty.
AVAILABLE_TIME_WEBHOOK_URL=
# Optional: separate webhook for plotted schedules. Defaults to SHEET_WEBHOOK_URL if empty.
SCHEDULE_WEBHOOK_URL=
